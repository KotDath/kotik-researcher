# Tasks: project-workspaces-pi-chats

## 1. Спайк: pi SDK в упакованной сборке (БЛОКЕР — до UI-работ)

- [x] 1.1 Добавить зависимость `@earendil-works/pi-coding-agent` строго `0.82.1` (без диапазона) и `electron-builder` в devDependencies
- [x] 1.2 Перевести сборку main-процесса на ESM (конфиг electron-vite); preload оставить работающим с contextBridge; `pnpm dev` и `pnpm build` работают
- [x] 1.3 Минимальный спайк в main: изолированный каталог данных pi (внутри userData), создание сессии через SDK в тестовой директории, один `prompt` → ответ от LLM выводится/логируется (работает в `pnpm dev`)
- [x] 1.4 Настроить electron-builder: targets Linux/macOS/Windows, `asarUnpack` для `**/*.node` (нативный модуль photon-node)
- [x] 1.5 Верифицировать наличие prebuilds `@silvia-odwyer/photon-node@0.3.4` под Linux, macOS, Windows (артефакты пакета)
- [x] 1.6 Упакованная сборка на Linux запускается, спайк из 1.3 получает ответ от LLM в packaged-режиме
- [x] 1.7 Зафиксировать результат спайка в decisions.md (строка с датой: успех/починка/эскалация). При провале — чинить упаковку (asarUnpack, явное копирование `.node`, пересборка); если чинится только «грязно» — СТОП фичи, отдельное решение о Tauri с RPC-sidecar

## 2. Инфраструктура pi в main-процессе

- [x] 2.1 Сервис pi-runtime в main: `AgentSession` на чат (`cwd` = директория проекта), изолированный каталог данных pi внутри userData (`PI_CODING_AGENT_DIR`). _Реализовано как сессия-на-чат вместо одного AgentSessionRuntime на проект — отклонение и обоснование в decisions.md_
- [x] 2.2 Lifecycle сессий: загружены активный и генерирующие чаты; подписки на события при активации чата; dispose неактивных при смене проекта
- [x] 2.3 Отложенный dispose: runtime с активной генерацией живёт до `agent_end`, затем dispose (фоновая дозапись при переключении проекта)
- [x] 2.4 Хранилище настроек: ключи, зашифрованные safeStorage (fallback 0600 + флаг недоступности шифрования), base-url, модель по умолчанию; атомарная запись
- [x] 2.5 Проброс конфигурации в pi: `setRuntimeApiKey` при создании runtime и при изменении настроек (без перезапуска); `authPath`/`modelsPath` внутри изолированного каталога; генерация `models.json` для custom base-url/модели
- [x] 2.6 IPC-слой и preload API (`window.api`): projects/chats/messages/settings по контракту design.md (решение 4), события стриминга через `webContents.send`

## 3. Проекты

- [x] 3.1 Хранилище недавних проектов: JSON в userData (`{path, lastOpenedAt}`), атомарная запись, сортировка по дате, поднятие при повторном открытии без дублей
- [x] 3.2 Открытие директории через системный диалог; создание проекта (родительская папка + имя → mkdir, конфликт имён — сообщение)
- [x] 3.3 Пометка «недоступен» для удалённых/перемещённых директорий; сообщение + «убрать из списка» при попытке открыть
- [x] 3.4 «Убрать из списка» для любого проекта (директория не трогается)
- [x] 3.5 UI: список недавних проектов / экран выбора проекта, «сменить проект» в sidebar

## 4. Чаты

- [x] 4.1 Список чатов проекта из `SessionManager.list(cwd)`, сортировка по последней активности; авто-создание первого чата в пустом проекте
- [x] 4.2 Новый чат (чистая сессия), переименование (`appendSessionInfo`), поиск/фильтр по имени
- [x] 4.3 Удаление чата: выгрузка из runtime → `shell.trashItem` (fallback `fs.unlink`); удаление активного/последнего чата по сценариям дельты
- [x] 4.4 Экран чата: отправка сообщений, стриминг ответа (text_delta), история из сессии при открытии
- [x] 4.5 Tool-вызовы сворачиваемыми блоками (имя tool, статус выполнения) между сообщениями
- [x] 4.6 Карточка ошибки в ленте (причина + «повторить»); ввод не блокируется; отправка без ключей → карточка ошибки провайдера
- [x] 4.7 Фоновая генерация: при переключении чата UI отписывается, сессия продолжает писаться; при возврате — снапшот `session.messages` + доклейка дельт без дублей

## 5. Настройки

- [x] 5.1 Экран настроек (доступен в любой момент): поля API-ключей, base-url, выбор модели по умолчанию; предупреждение при недоступном safeStorage
- [x] 5.2 Сохранение настроек применяется к живому runtime без перезапуска (повторная отправка с новым ключом работает)

## 6. Проверка

- [x] 6.1 `pnpm typecheck` и `pnpm lint` проходят
- [x] 6.2 Smoke: `pnpm build` собирается, упакованный артефакт (electron-builder) запускается
- [x] 6.3 E2E-приёмка (vision): создать проект из приложения → авто-созданный чат → отправить сообщение → стримящийся ответ со сворачиваемыми tool-блоками → перезапуск → проект в списке недавних, чат и история на месте, диалог продолжается
- [x] 6.4 Проход по сценариям дельта-спек (project-workspaces, pi-chat-sessions, llm-provider-settings): каждый сценарий проверен вручную, расхождения зафиксированы
