# Delta: llm-provider-settings

## ADDED Requirements

### Requirement: Per-provider уровень thinking

Система ДОЛЖНА позволять в Settings выбирать уровень thinking для каждого
провайдера отдельно. Выбранный уровень ДОЛЖЕН запоминаться per-provider и
переживать перезапуск приложения: переключение на провайдера применяет его
сохранённый уровень. Настройка уровня ДОЛЖНА быть видна в Settings всегда,
независимо от того, поддерживает ли текущая модель thinking.

#### Scenario: Уровень запоминается per-provider

- **GIVEN** для провайдера A выбран уровень high, для провайдера B — off
- **WHEN** пользователь переключает модель по умолчанию с провайдера A на B
- **THEN** для сессий на провайдере B действует уровень off
- **AND** при возврате на провайдера A снова действует уровень high

#### Scenario: Выбор переживает перезапуск

- **GIVEN** для провайдера выбран уровень medium
- **WHEN** пользователь перезапускает приложение
- **THEN** в Settings для этого провайдера по-прежнему выбран medium

#### Scenario: Настройка видна для модели без поддержки thinking

- **GIVEN** текущая модель провайдера не поддерживает thinking
- **WHEN** пользователь открывает Settings
- **THEN** настройка уровня thinking отображается (уровень можно выбрать и
  сохранить; на чат это не оказывает видимого эффекта)

### Requirement: Набор уровней ограничен возможностями модели

Список доступных уровней в настройке ДОЛЖЕН состоять из «off» и уровней,
поддерживаемых текущей моделью провайдера (по API pi SDK). Для провайдера без
сохранённого выбора дефолтом ДОЛЖЕН быть включённый уровень (из диапазона
low/medium, при их доступности), чтобы reasoning был виден «из коробки».

#### Scenario: Список отражает возможности модели

- **GIVEN** текущая модель провайдера поддерживает уровни low и medium
- **WHEN** пользователь открывает настройку уровня для этого провайдера
- **THEN** в списке доступны off, low и medium — и только они

#### Scenario: Дефолт нового провайдера — включённый уровень

- **GIVEN** для провайдера никогда не выбирался уровень thinking, а его текущая
  модель поддерживает low и medium
- **WHEN** провайдер используется впервые
- **THEN** действует включённый уровень (low или medium), а не off

### Requirement: Живое применение уровня thinking

Изменение уровня thinking в Settings ДОЛЖНО применяться ко всем активным
сессиям без перезапуска приложения и без пересоздания чата: новый уровень
действует со следующего запроса. Смена уровня НЕ ДОЛЖНА прерывать текущий
стрим: текущий ответ завершается со старым уровнем.

#### Scenario: Новый уровень действует со следующего запроса

- **GIVEN** открыт чат, для провайдера выбран уровень off
- **WHEN** пользователь меняет уровень на high и сразу отправляет сообщение
- **THEN** ответ на это сообщение генерируется с уровнем high без перезапуска
  приложения и без пересоздания чата

#### Scenario: Смена уровня не рвёт текущий стрим

- **GIVEN** идёт генерация ответа с уровнем low
- **WHEN** пользователь меняет уровень на high во время стриминга
- **THEN** текущий ответ завершается без прерывания
- **AND** уровень high действует со следующего запроса
