# Decisions: chat-reasoning-stream

<!-- Одна строка на решение: дата · контекст · выбор · почему.
     Фиксируются отвергнутые варианты тоже — они ценнее всего теряются. -->

- 2026-07-28 · скоуп фичи · проброс thinking + глобальная настройка уровня · пользователь: «Проброс + глобальная настройка», пер-чат не нужен
- 2026-07-28 · отвергнуто: пер-чат и пер-модель (внутри провайдера) переопределения уровня · лишняя сложность UI/стейта, пользователь явно отказался
- 2026-07-28 · гранулярность настройки · свой уровень на каждого провайдера, запоминается · пользователь: «у каждого провайдера свой уровень ризонинга, что ему доступен»
- 2026-07-28 · набор уровней в UI · весь набор SDK (off + getAvailableThinkingLevels(), включая minimal/xhigh) · верификация-вопрос: пользователь подтвердил фильтрацию возможностями модели вместо жёстких 5 уровней из vision
- 2026-07-28 · дефолт уровня для нового провайдера · включённый (low/medium) · видимость фичи «из коробки» важнее экономии токенов
- 2026-07-28 · применение смены уровня · немедленно ко всем активным сессиям, со следующего запроса, стрим не рвём · живость настройки важнее консистентности сессии
- 2026-07-28 · interleaved thinking · каждая порция — отдельный блок в хронологической позиции · хронологическая точность важнее простоты UI
- 2026-07-28 · отвергнуто: агрегация всех порций reasoning ответа в один блок · теряется хронология относительно tool-вызовов
- 2026-07-28 · сворачивание · развёрнут при стриминге, auto-collapse по завершении, из истории всегда свёрнут · компактность истории важнее сохранения UI-состояния
- 2026-07-28 · оборванный reasoning · виден в живой ленте, в историю не пишется; auto-retry заменяет без дублей · чистота истории важнее полноты (LRN-20260728-002)
- 2026-07-28 · модель без thinking · молча, без плейсхолдеров; настройка видна всегда · зафиксировано как норма, снимает блокер «а вдруг событий нет»
- 2026-07-28 · хедер блока · лейбл + длительность («Thinking · 12s»), тикает вживую, хранится · паттерн Claude UI / OpenCode по запросу пользователя
- 2026-07-28 · оформление блока · приглушённый серый (--text-dim), курсив, линия-отступ, иконка-лампочка; тема НЕ меняется · скриншот-референс OpenCode; приложение уже тёмное
- 2026-07-28 · персистентность длительности · sidecar JSON (sessionFile+contentIndex → durationMs) · SDK длительность не хранит; вычисление из timestamp'ов jsonl отвергнуто — хрупко при tool-вызовах и compaction
- 2026-07-28 · отвергнуто: ручная таблица маппинга уровней per-провайдер в коде · getAvailableThinkingLevels() уже отражает возможности модели; ручная таблица устареет с обновлением pi
- 2026-07-28 · спайк 1.1 (SPIKE_HEADLESS=thinking, deepseek/deepseek-v4-pro, setThinkingLevel('low')→кламп в 'high', живой prompt) · события thinking_start/delta/end ПРИХОДЯТ в message_update.assistantMessageEvent: starts=1 ends=1 chars=166 contentIndex=0 durationMs≈1905; в истории assistant.content=[thinking,text], thinking-часть персистируется · допущение design.md подтверждено, молчаливый режим не активен для текущей модели
- 2026-07-28 · спайк 1.2 (deepseek-v4-pro и deepseek-v4-flash) · supportsThinking()=true для обеих; getAvailableThinkingLevels()=["off","high","max"] — «off» ВХОДИТ в список (расходится с допущением design.md «off не входит»), реальные включённые уровни модели только high/max; setThinkingLevel('low') клампится SDK до 'high' · следствие для реализации: UI строит список как off + available с дедупликацией off; дефолт нового провайдера — первый из low/medium, иначе первый НЕ-off из available; для моделей без поддержки — supportsThinking()=false, чат молчит по спеке
- 2026-07-28 · review-fix 1 · ключ sidecar длительностей — (sessionFile, messageTimestamp, contentIndex), не (sessionFile, contentIndex) · contentIndex повторяется в каждом assistant-сообщении, длительности перезаписывали друг друга; timestamp сообщения создаётся один раз при старте стрима в pi-ai и персистируется в jsonl — спайк подтвердил partial.timestamp на thinking_end === timestamp в истории (1785265750653 = 1785265750653); спайк: 3 ответа → 3 разных ключа
- 2026-07-28 · review-fix 2/3 · дедуп retry и закрытие блока — scan-based поиск streaming-блока, не «последний item» · message_end со stopReason error/aborted НЕ закрывает thinking-блок (иначе следующий thinking_start дублировал бы); блок закрывается финальным agent_end/error или заменяется thinking_start новой попытки где бы он ни был в ленте — зависания нет, т.к. финальный agent_end гарантирован SDK
- 2026-07-28 · review-fix 4 · устаревший saved-уровень (нет в списке модели) · показывается disabled-опцией «<level> (не поддерживается моделью)», в активный список не добавляется · при применении SDK клампит (setThinkingLevel) — отдельный resolve в main не нужен
- 2026-07-28 · review-fix 5 · ручное сворачивание во время стриминга запрещено · кнопка-хедер disabled пока streaming=true; auto-collapse и ручное управление после завершения без изменений
- 2026-07-28 · review-fix 6 · пустая/redacted порция в живой ленте · выбрано удаление блока при закрытии, если текст пуст (closeThinking), а не ленивое создание на первый delta · одинаково покрывает thinking_end и обрыв через agent_end/error
- 2026-07-28 · review-fix цикла 2 · snapshot во время стриминга reasoning · partial-сообщение появляется в session.messages только на message_end, поэтому handle накапливает текст порции (openThinking.text) и список свежезавершённых порций (completedThinking, чистится на message_end/agent_start); snapshot достраивает их: живая — streaming с startedAt (таймер тикает с правильной базы), завершённая — закрытой с durationMs · замечено в спайке: OpenAI-стиль провайдеров (deepseek) шлёт text_delta ДО thinking_end — порядок «thinking_end → первый text» не гарантирован, scan-based хелперы renderer это покрывают
- 2026-07-28 · review-fix цикла 3 · селектор уровня — СТРОГО off + уровни модели, без опции «По умолчанию» · опция обещала включённый дефолт даже для модели без thinking и нарушала «список отражает возможности модели»; дефолт обеспечивает main-side resolveThinkingLevel, в UI без сохранённого выбора показывается вычисленный effective-уровень (та же формула: low/medium → первый не-off → off) — отображение совпадает с фактическим поведением
- 2026-07-28 · review-fix цикла 3 · гонка snapshot/event в ChatArea · выбран вариант (б): буферизация событий, пришедших во время pending snapshot, и применение их поверх snapshot через общий applyEvent с дедупом по seq · отвергнуто (а) discard+refetch — циклы перезапроса и мерцание; отвергнуто (в) сериализация в main — меняет IPC-протокол ради проблемы, решаемой на стороне потребителя; критерий: события не теряются (буфер) и не применяются дважды (seq-дедуп внутри applyEvent)

## Сессии агентов

- implementer: ses_0560ed511ffem8wWSE5aiMzwJE (реализация 2026-07-28, 4 коммита, typecheck/lint/build зелёные)
- reviewer: ses_055ed24c4ffezJ0XPeeNIq2ufa (цикл 1, 2026-07-28 → CHANGES_REQUESTED: 3 блокера — ключ sidecar без id сообщения, retry-dedup после message_end, agent_end(willRetry) не шлёт закрытие; 2 major — saved-уровень в списке, сворачивание во время стриминга; 1 minor — пустой/redacted блок в ленте. Цикл 2, 2026-07-28 → все 6 фиксов подтверждены, CHANGES_REQUESTED: 1 блокер — snapshot активного чата всегда streaming:false, потеря ленты reasoning при переключении чатов)
- reviewer (финальное приёмочное, свежий): ses_055ce33e3ffeptUsjfVnBwiDuT (цикл 3, 2026-07-28 → CHANGES_REQUESTED: 1 блокер — лишняя selectable-опция «По умолчанию (включённый)» в SettingsView:223, сценарий «список отражает возможности модели»; 1 major — race snapshot/event в ChatArea:95-100, callback snapshot откатывает live-событие, потерянный thinking_start убивает стрим в UI; 17/19 сценариев покрыто)
