# Decisions: ui-verification-infra

<!-- Одна строка на решение: дата · контекст · выбор · почему.
     Фиксируются отвергнутые варианты тоже — они ценнее всего теряются. -->

- 2026-07-29 · скоуп первого захода · всё сразу (Vitest + Playwright E2E + visual + agent-driven + ui-reviewer + документация) · закрыть LRN-20260729-001 системно, а не частично; цена откладывания — повторение инцидента
- 2026-07-29 · инструмент E2E · Playwright Test (`_electron` API) + `@playwright/mcp` · единый движок для CI-тестов и агентской верификации; рекомендован Electron-доками; WebDriverIO отвергнут: нет MCP-интеграции, сложнее старт
- 2026-07-29 · режимы агент-driven верификации · два режима (быстрый: dev-server + MCP; полный: live Electron через CDP + MCP) с явным разделением в AGENTS.md · быстрый — секунды для итераций implementer'а; полный — реальная main-renderer интеграция для финальной верификации
- 2026-07-29 · отвергнуто: унификация в один режим агента · быстрый не ловит баги IPC/main; полный слишком медленный для итераций
- 2026-07-29 · ui-reviewer · отдельный субагент (`.opencode/agents/ui-reviewer.md`), deny:edit, PASS/FAIL · reviewer проверяет код против спеки, ui-reviewer — UI против `docs/ui-review.md`; разные компетенции, разный инструментарий
- 2026-07-29 · отвергнуто: расширение reviewer'а дополнительным UI-чеком · reviewer уже проверяет код, self-review bias удваивается; deny:edit ui-reviewer'а — дополнительная защита
- 2026-07-29 · FAIL от ui-reviewer · hard gate, блокирует approve без исключений · максимальная защита от визуальных багов; человек видит и решает
- 2026-07-29 · кто вызывает ui-reviewer · оркестратор — диспетчер: reviewer сообщает «нужна UI-проверка», оркестратор вызывает ui-reviewer · reviewer не меняется, оркестратор координирует
- 2026-07-29 · цикл generator/evaluator · максимум 3 итерации, после 3-го FAIL — эскалация к человеку · предотвращает бесконечную «полировку» агентом субъективных деталей (урок Addy Osmani)
- 2026-07-29 · baseline-скриншоты · 5–10 ключевых экранов, в git, только ручное обновление (`--update-snapshots`) · автообновление запрещено: агент может сломать UI и обновить эталон, получив зелёный тест
- 2026-07-29 · отвергнуто: автообновление baseline в CI или агентом · главный риск visual regression — self-reinforcing ошибка: сломал → обновил → «тесты зелёные»
- 2026-07-29 · количество baseline-скриншотов · 5–10, не полное покрытие · совет Kent C. Dodds: один E2E happy path для критичного сценария, остальное — интеграционными тестами; снижает хрупкость и maintenance-нагрузку
- 2026-07-29 · файловая структура тестов · `tests/` в корне, поддиректории `unit/`, `e2e/`, `visual/` · все тесты в одном месте, вне `src/`; отвергнуто: `src/__tests__/` — тесты не код приложения
- 2026-07-29 · Playwright конфигурация · один `playwright.config.ts` в корне, два проекта внутри (`e2e`, `visual`) · единая точка входа, общий fixture
- 2026-07-29 · отвергнуто: раздельные конфиги для e2e и visual · дублирование fixture и общей конфигурации
- 2026-07-29 · два режима E2E-тестов · `pnpm test:e2e` (build + test, для reviewer'а) и `pnpm test:e2e:quick` (dev + test, для implementer'а) · build-режим гарантирует проверку production-сборки; quick-режим ускоряет итерации
- 2026-07-29 · отвергнуто: только build-режим E2E · каждая итерация implementer'а требовала бы полной сборки
- 2026-07-29 · Vitest workspace · два проекта (node + web tsconfig) · разрешает конфликт tsconfig (node vs DOM) без ручного переключения environment
- 2026-07-29 · стратегия селекторов · `data-testid` атрибуты + `page.getByTestId()` · защита от хрупкости CSS-селекторов и DOM-структуры; отвергнуто: ARIA-селекторы — не дают точечной адресации конкретных компонентов
- 2026-07-29 · моки нативных диалогов · `electronApp.evaluate()` для подмены `dialog.showOpenDialog` и аналогов · Playwright не перехватывает системные диалоги; отвергнуто: отдельный IPC-канал для тестов — добавляет код в production только для тестов
- 2026-07-29 · судьба spike.ts / SPIKE_HEADLESS · удалить · новый стек Playwright E2E + MCP + Vitest покрывает все сценарии системно; не плодим зоопарк инструментов
- 2026-07-29 · MCP-сервер · конфиг в `opencode.json` (`mcp.playwright`) · MCP всегда доступен агентам без ручного запуска; ui-reviewer, implementer, reviewer используют одни и те же инструменты
- 2026-07-29 · контракт implementer'а о renderer · строка `Change touches: renderer` (или `main`, `both`) в ответе оркестратору · implementer анализирует свой дифф, а не оркестратор; строка — простой парсимый контракт
- 2026-07-29 · ужесточение протокола идеатора · обязательное покрытие всех измерений карты в vision.md, оркестратор проверяет полноту · унификация качества vision.md; предотвращает пропуск измерений (Risks, Edge cases) до spec-writer'а
- 2026-07-29 · интеграция в SDD · implementer (быстрая самопроверка: unit + E2E quick + dev-server MCP) → reviewer (полная: вызывает ui-reviewer через оркестратора) → оркестратор (диспетчер, проверяет hard gates) · чёткое разделение ответственности на каждом этапе SDD-цикла
- 2026-07-29 · MCP-путь в opencode.json · `mcp.playwright` (НЕ `mcp.servers.playwright`) · схема opencode.ai/config.json не имеет уровня servers; невалидный конфиг = отказ запуска opencode (спека синхронизирована оркестратором)
- 2026-07-29 · версия vitest · закрепить ^3.2.7 · Vitest 4 удалил поддержку `vitest.workspace.ts` (требуется спекой): файл не загружается, `test.workspace` кидает ошибку
- 2026-07-29 · переключение режимов MCP (review-fix, блокер 1) · два именованных сервера `playwright` + `playwright-cdp` в opencode.json · один MCP-процесс не переключается автономный↔CDP; ручная переконфигурация в живой сессии невозможна — агент выбирает сервер по префиксу инструментов
- 2026-07-29 · агентский Electron (review-fix, блокер 2) · изоляция как в E2E (`--e2e` + `E2E_USER_DATA_DIR`, env allowlist) + сид-данные (recent-projects, проект, чат) · полный режим не должен видеть/мутировать/скриншотить реальные проекты и API-ключи; пустое приложение проверять нечего
- 2026-07-29 · test:e2e:quick (review-fix, блокер 3) · `electron-vite build` перед прогоном + renderer из dev-server · quick на stale main/preload не проверял изменения main; сборка ~1с — цена приемлемая, «без пересборки» теперь = «без пересборки renderer'а»
- 2026-07-29 · мок api для visual-состояний (review-fix, блокер 4) · `E2E_MOCK_API` в preload (`src/preload/mock-api.ts`) · contextBridge экспонирует read-only window.api — подмена снаружи невозможна; preload — единственная точка до рендера; visual снова внутри Electron; обход через Chromium отвергнут (нарушал требование «оба проекта через _electron.launch»)
- 2026-07-29 · env в тестах (review-fix, major 9) · allowlist вместо denylist `*_API_KEY` · denylist пропускал `*_TOKEN`/`*_SECRET`/AWS-креды к реальному SDK → риск сетевых вызовов и расходов; allowlist детерминизирует исход (карточка ошибки)
- 2026-07-29 · чаты в E2E без LLM · seed session-файлов pi (v3) в изолированный userData · SDK `_persist` не пишет сессию без assistant-сообщения, пустой чат исчезает при переключении (disposeIfIdle) — два чата без LLM недостижимы через UI; pass навигации в цикле 0 был недетерминированным
- 2026-07-29 · контракт активного чата (review-fix, блокер 7) · `aria-current` вместо CSS-класса · класс — внутренняя деталь реализации; aria-current — наблюдаемый пользователем признак
- 2026-07-29 · решение A: один MCP вместо двух · единый сервер `mcp.playwright` с `--cdp-endpoint :9222` в opencode.json; оба агентских режима — Electron на CDP · `@playwright/mcp` не переключается автономный↔CDP в одном процессе; два сервера создавали путаницу префиксов (`playwright_*` vs `playwright-cdp_*`); унификация на CDP-only убирает путаницу и Chromium-режим, который не проверял main-интеграцию (LRN-20260729-001)
- 2026-07-29 · отвергнуто: автономный Chromium-режим MCP против dev-server · не проверяет main/preload/IPC — баги на этих слоях невидимы; CDP к Electron даёт полноценную интеграцию в обоих режимах (dev и prod)
- 2026-07-29 · решение B: невидимые окна · `show: false` + `backgroundThrottling: false` при `--e2e`; скриншоты через CDP `Page.captureScreenshot` из композитора · окна не крадут фокус пользователя при тестовых/агентских прогонах; Xvfb избыточен для локального использования без CI
- 2026-07-29 · отвергнуто: Xvfb · избыточно: скрытые окна + CDP-скриншоты решают задачу без виртуального дисплея; Xvfb добавляет зависимость и сложность, оправдан только в CI
- 2026-07-29 · решение C: трёхагентный конвейер · implementer (только код + unit) → test-runner ∥ reviewer → ui-reviewer; test-runner на flash (opencode-go/deepseek-v4-flash), ui-reviewer на k3 (vision); reviewer — логика-only · implementer на k3 дорог для механических прогонов e2e/visual; flash дёшев и быстр; reviewer не должен гонять тесты (test-runner уже сделал); три независимых deny:edit агента — максимальная защита от self-review bias
- 2026-07-29 · отвергнуто: implementer с полной самопроверкой (e2e + visual + agent) · тяжело для k3 (токены, время); привычка перепроверять себя канализируется в дешёвые unit-тесты; e2e/visual — территория test-runner'а
- 2026-07-29 · отвергнуто: reviewer, прогоняющий тесты · размывает ответственность и удваивает время (тесты уже пройдены test-runner'ом); reviewer читает отчёт test-runner'а, но не дублирует его работу
- 2026-07-29 · параллельность test-runner ∥ reviewer · запускаются одновременно, независимы · test-runner не зависит от reviewer'а (только механический прогон) и наоборот; экономия времени: два агента отрабатывают за время одного
- 2026-07-29 · triple-PASS gate · approve только при PASS test-runner + reviewer + ui-reviewer (последний — при renderer/both) · любой FAIL останавливает конвейер; максимальная защита от визуальных, логических и тестовых багов
- 2026-07-29 · модель ui-reviewer · kimi-for-coding/k3 · нужна поддержка изображений (vision) для оценки скриншотов; тяжёлая модель оправдана важностью финальной визуальной верификации
- 2026-07-29 · пересмотр решения C после ревью метасистемы · отдельный test-runner отменён; deterministic pnpm-команды выполняются по tasks.md, implementer и app-tester используют Flash, reviewer всегда GPT-5.6 Sol/medium, ui-reviewer K3 · запуск фиксированной команды не требует LLM-роли; генеративная модель оправдана для написания тестов, exploratory app testing и визуального verdict
- 2026-07-29 · reviewer protocol · blocker/major требуют evidence; implementer отвечает ACCEPT/DISPUTE/PRE_EXISTING, спор adjudicates оркестратор, архитектурный спор — архитектор · замечание модели не является hard gate без доказуемого нарушения

## Сессии агентов

- implementer: ses_0534744e9ffe0U9ZkhEr9o6lAC (реализация 2026-07-29, 12 групп, 53 unit + 8/8 E2E ×2 + 7/7 visual зелёные, 4 задокументированных отклонения; 12.11 не отмечена — требует живой сессии оркестратора)
- reviewer: ses_052d97313ffeDV7hSBoYV7mLgq (цикл 1, 2026-07-29 → CHANGES_REQUESTED: 8 блокеров — CDP-режим MCP не работает, агентский Electron без изоляции userData, quick-прогон со stale out/, visual-состояния в Chromium вне Electron fixture, 5.7 не «нет чатов», селекторы option.nth(1) и CSS-класс active, фильтр секретов только *_API_KEY; 1 major — реальные сетевые вызовы SDK; 1 minor — teardown без try/finally. Подтверждено: mcp.playwright корректен, спека синхронизирована; vitest ^3.2.7 легитимен)
