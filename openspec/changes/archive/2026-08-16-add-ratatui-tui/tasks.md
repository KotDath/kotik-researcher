# Tasks: add-ratatui-tui

## 1. Подготовка

- [x] 1.1 Добавить зависимости `ratatui` и `crossterm` (актуальные совместимые версии) в `src-tauri/crates/kotik-cli/Cargo.toml`; зафиксировать версии
- [x] 1.2 Создать скелет модулей `app.rs`, `ui.rs` в `kotik-cli/src/` (пустые структуры, крейт компилируется)

## 2. Состояние приложения (app.rs, без IO)

- [x] 2.1 Реализовать `App`: история сообщений (пользователь/ассистент/системные ошибки — системные не попадают в контекст агента), буфер ввода с позицией курсора, `scroll: Option<u16>`, флаг `streaming`
- [x] 2.2 Реализовать редьюсеры: `on_key` (редактирование, Enter = submit, Shift+Enter/Alt+Enter/Ctrl+J = перевод строки), `on_chunk`, `on_stream_error`, `submit`; Enter во время стриминга игнорируется (D3)
- [x] 2.3 Реализовать логику скролла: PgUp/PgDown/↑/↓ меняют смещение, достижение нуля → `None` (прилипание к низу) (D5)
- [x] 2.4 Юнит-тесты на редьюсеры: ввод/переводы строк, submit формирует историю, ошибка стрима → системное сообщение и история агента не загрязнена, скролл прилипает/отлипает

## 3. Отрисовка (ui.rs)

- [x] 3.1 Функция отрисовки кадра: layout — область истории + поле ввода; история через `Paragraph` с `Wrap` и скроллом из `App`
- [x] 3.2 Визуальное различение ролей (пользователь/ассистент/системная ошибка) и приветственная подсказка про клавиши (Enter — отправить, Alt+Enter/Ctrl+J — новая строка, Ctrl+C или /exit — выход)
- [x] 3.3 Тест отрисовки через `ratatui::backend::TestBackend`: кадр содержит сообщения и буфер ввода

## 4. Цикл событий и терминал (main.rs)

- [x] 4.1 Инициализация терминала: raw mode + alternate screen с RAII-guard восстановления (включая панику) (D6, риски)
- [x] 4.2 `tokio` цикл: `select!` между `crossterm::event::EventStream` и `mpsc`-каналом чанков; на submit — `tokio::spawn` стрима `stream_reply` с отправкой событий в канал (D3)
- [x] 4.3 Отсутствие `DEEPSEEK_API_KEY`: сообщение в stderr и exit code 1 до поднятия TUI
- [x] 4.4 Выход: `/exit` в поле ввода и Ctrl+C корректно закрывают TUI, терминал восстановлен, код выхода 0
- [x] 4.5 Удалить старый REPL-код (read_line/stdout-печать) из `main.rs`

## 5. Интеграционная проверка и документация

- [x] 5.1 Ручной прогон: `cargo run -p kotik-cli` — отправка, стриминг, скролл во время стриминга, многострочное сообщение, ошибка сети (невалидный ключ/отключённая сеть) как системное сообщение, выход
- [x] 5.2 Definition of Done: `cargo test`, `cargo clippy --workspace -- -D warnings`, `cargo fmt -- --check`, `npm run lint`, `npm run build` — все зелёные
- [x] 5.3 Обновить `AGENTS.md`: карта проекта (структура `kotik-cli`), таблица зависимостей (ratatui, crossterm), убрать ratatui из «осознанно отложено»
